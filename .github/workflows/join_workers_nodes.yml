name: Join Worker Nodes to Master

on:
  workflow_dispatch:

jobs:
  join-worker:
    name: Join Worker to Master Node
    runs-on: ubuntu-latest
    strategy:
      matrix:
        worker_ip:
          - ${{ secrets.EC2_PUBLIC_IP_WORKER1 }}
          - ${{ secrets.EC2_PUBLIC_IP_WORKER2 }}
        worker_user:
          - ${{ secrets.EC2_USER_WORKER1 }}
          - ${{ secrets.EC2_USER_WORKER2 }}

    steps:
      - name: Install SSH Client
        run: |
          sudo apt-get update
          sudo apt-get install -y openssh-client

      - name: Configure Worker Node and Join Cluster
        env:
          SSH_PRIVATE_KEY: ${{ secrets.KEY_BLOC4 }}
          JOIN_CLUSTER: ${{ secrets.JOIN_CLUSTER }}
        run: |
          # Create SSH key file
          echo "$SSH_PRIVATE_KEY" > private_key.pem
          chmod 600 private_key.pem

          # SSH into worker node and configure
          ssh -o StrictHostKeyChecking=no -i private_key.pem ${{ matrix.worker_user }}@${{ matrix.worker_ip }} <<'EOF'
            # Add user to microk8s group
            sudo usermod -a -G microk8s ${{ matrix.worker_user }}

            # Create .kube directory
            mkdir -p ~/.kube
            sudo chown -R ${{ matrix.worker_user }} ~/.kube

            # Activate group change
            newgrp microk8s

            # Join cluster
            $JOIN_CLUSTER
          EOF
