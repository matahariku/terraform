name: Setup Multi-Cluster with Minikube

on:
  workflow_dispatch:

jobs:
  setup-multi-cluster:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout repository
      - name: Checkout Repository
        uses: actions/checkout@v3

      # 2. Install Minikube and Kubectl
      - name: Install Minikube and Kubectl
        run: |
          sudo apt-get update -y
          sudo apt-get install -y apt-transport-https curl
          sudo curl -LO https://storage.googleapis.com/minikube/releases/latest/minikube-linux-amd64
          sudo install minikube-linux-amd64 /usr/local/bin/minikube
          sudo curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
          sudo install kubectl /usr/local/bin/kubectl

      # 3. Start Minikube Cluster for AWS
      - name: Start Minikube Cluster (AWS)
        run: |
          minikube start --profile aws-cluster --kubernetes-version=v1.26.0 --cpus=2 --memory=2g
          kubectl config use-context aws-cluster

      # 4. Deploy Resources to AWS Cluster
      - name: Deploy MongoDB to AWS Cluster
        run: |
          kubectl create namespace aws-namespace
          kubectl apply -f k8s/mongo-deployment-aws.yaml -n aws-namespace

      # 5. Start Minikube Cluster for OVH
      - name: Start Minikube Cluster (OVH)
        run: |
          minikube start --profile ovh-cluster --kubernetes-version=v1.26.0 --cpus=2 --memory=2g
          kubectl config use-context ovh-cluster

      # 6. Deploy Resources to OVH Cluster
      - name: Deploy MongoDB to OVH Cluster
        run: |
          kubectl create namespace ovh-namespace
          kubectl apply -f k8s/mongo-deployment-ovh.yaml -n ovh-namespace

      # 7. Verify Deployments
      - name: Verify AWS Deployment
        run: |
          kubectl config use-context aws-cluster
          kubectl get pods -n aws-namespace
      - name: Verify OVH Deployment
        run: |
          kubectl config use-context ovh-cluster
          kubectl get pods -n ovh-namespace
