name: Test Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted
  

    steps:
      - name: Test Runner Connection
        run: |
          echo "Runner aktif dan berjalan!"
          uname -a
